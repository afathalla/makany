<div class="nav">
   <% if current_person %>
   <%= link_to "Home", person_path(current_person.id) %>
   <%= link_to "Edit Profile", edit_person_path(:current) %>
   <%= link_to "Logout", logout_path %>
   <% else %>
   <%= link_to "Register", new_person_path %>
   <%= link_to "Login", login_path %>
   <%end%>
   <a href="#">About</a>
  </div>